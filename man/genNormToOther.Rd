% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genNormToOther.R
\name{genNormToOther}
\alias{genNormToOther}
\title{This function generate single arm(having multiple endpoint) responses given the normal scale correlation matrix}
\usage{
genNormToOther(
  nSubject = 100,
  lEpType = list("Continuous", "Continuous"),
  mNormCorr = matrix(c(1, 0.5, 0.5, 1), nrow = 2),
  mNormSigma = matrix(c(1, 0.5, 0.5, 1), nrow = 2),
  vNormMean = c(0.1, 0.4),
  vProp = c(0.1, 0.2),
  nSeed = 123
)
}
\arguments{
\item{nSubject}{number of response to generate}

\item{lEpType}{list with endpoint types}

\item{mNormCorr}{correlation matrix in normal scale(when marginals are not normal)}

\item{mNormSigma}{covariance matrix in normal scale(when marginals are all normal)}

\item{vNormMean}{vector of normal means(when marginals are all normal)}

\item{vProp}{vector of proportions(when marginals are all binary)}

\item{nSeed}{seed}
}
\value{
array of responses(columns = endpoints, Rows = subject)
}
\description{
This function generate single arm(having multiple endpoint) responses given the normal scale correlation matrix
}
