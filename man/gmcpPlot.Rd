% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MISgmcpPlot.R
\name{gmcpPlot}
\alias{gmcpPlot}
\title{Function to plot the graph by using initial weights and the transition matrix.}
\usage{
gmcpPlot(
  WI = c(1/4, 1/4, 1/4, 1/4),
  G = matrix(c(0, 0.75, 0.25, 0, 0.75, 0, 0, 0.25, 1, 0, 0, 0, 0, 1, 0, 0), nrow = 4,
    byrow = TRUE),
  nameHypotheses,
  hGroup,
  cordinates
)
}
\arguments{
\item{WI}{Vector of Initial Weights for Global Null(default = \code{rep(1/4,4)})}

\item{G}{Transition Matrix (default = \code{matrix(c(0,0.75,0.25,0,0.75,0,0,0.25,1,0,0,0,0,1,0,0),nrow = 4, byrow = T)})}

\item{nameHypotheses}{Vector to contain the name of the hypothesis(Optional)}

\item{hGroup}{vector to specify the groups of the hypothesis(Optional)}

\item{cordinates}{list to specify the node positions as co-ordinates(x,y) manually(Optional). The center of the plot is assumed to be (0,0)}
}
\description{
Function to plot the graph by using initial weights and the transition matrix.
}
\examples{
## Example-1: Primary and secondary type hypothesis
#Initial weights
WI <- c(1/2,1/2,0,0)

#Transition Matrix
G <- matrix(c(0,0.75,0.25,0,
              0.75,0,0,0.25,
              0,1,0,0,
              1,0,0,0),
            nrow = 4, byrow = TRUE)
#Group of hypothesis
hGroup <- c('P','P','S','S')

#Coordinates of the nodes(Hypothesis)
cordinates <- list(c(-1,1),c(1,1),c(-1,-1),c(1,-1))

#Plot
gmcpPlot(WI = WI, G = G, hGroup = hGroup, cordinates = cordinates)


##################################################################
## Example-2: Sequential rejection strategy for multi-arm study

#Initial weights
WI <- c(1/5,1/5,1/5,1/5,1/5)

#Transition Matrix
G  <- matrix(c(0,1/4,1/4,1/4,1/4,
               1/4,0,1/4,1/4,1/4,
               1/4,1/4,0,1/4,1/4,
               1/4,1/4,1/4,0,1/4,
               1/4,1/4,1/4,1/4,0),
             nrow = 5, byrow = TRUE)

#Plot
gmcpPlot(WI = WI, G = G)

}
